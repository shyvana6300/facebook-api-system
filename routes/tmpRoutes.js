const express = require('express');
const tmp_router = express.Router();
const accountController = require('../controller/Account');

tmpController.post('/tmpFunction', tmpController.tmpFunction);

module.exports = tmp_router;