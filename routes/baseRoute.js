const express = require('express');
const base_router = express.Router();
const baseController = require('../controller/accountController');

tmpController.post('/home', baseController.tmpFunction);

module.exports = tmp_router;